"""Minimal package entry for packaging the production code.

This package is intentionally minimal — it re-exports the public API from the
project's `src/` modules. Add functions here as the public surface grows.
"""

__all__ = [
    # Export package-level helpers here when ready, e.g. "get_system_status"
]
